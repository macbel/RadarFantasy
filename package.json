{
  "name": "radar-fantasy",
  "version": "3.13.4",
  "private": true,
  "description": "Scout de fichajes fantasy para web, Android e iOS 16+",
  "scripts": {
    "start": "node dev-server.js",
    "start:php": "node scripts/php-router.cjs",
    "test": "node tests\\dom-contract.test.js && node tests\\engine-smoke.test.js && node tests\\fixture-backend.test.cjs && node tests\\mobile-local-first.test.js && node tests\\mobile-cors.test.js && node tests\\android-update.test.cjs",
    "build:web": "node scripts/prepare-mobile-web.cjs",
    "mobile:copy": "npm run build:web && npx cap copy",
    "mobile:sync": "npm run build:web && npx cap sync",
    "mobile:android": "npm run build:web && npx cap sync android && npx cap open android",
    "mobile:ios": "npm run build:web && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/browser": "^7.0.5",
    "@capacitor/core": "^7.4.2",
    "@capacitor/local-notifications": "^7.0.0",
    "@capacitor/preferences": "^7.0.4"
  },
  "devDependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/cli": "^7.4.2",
    "@capacitor/ios": "^7.4.2"
  }
}
